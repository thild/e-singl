@model Neadm.Models.Curso

@{
    ViewBag.Title = $"Excluir curso \"{@Model.Nome}\"";
    Layout = "/Views/Shared/_LayoutAdmin.cshtml";
}

<section>
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h2 class="section-heading">@ViewBag.Title</h2>
                <hr class="hard">
                <h4>Tem certeza que deseja excluir o curso?</h4>
                <div>
                    <hr />
                    <dl class="dl-horizontal">
                        <dt>
                            @Html.DisplayNameFor(model => model.Codigo)
                        </dt>
                        <dd>
                            @Html.DisplayFor(model => model.Codigo)
                        </dd>
                        <dt>
                            @Html.DisplayNameFor(model => model.Nome)
                        </dt>
                        <dd>
                            @Html.DisplayFor(model => model.Nome)
                        </dd>
                    </dl>
                    <hr class="hard">
                    <form asp-controller="Curso" asp-action="Delete" method="post">
                        <div class="pull-right">
                            <div class="form-actions no-color">
                                <button type="submit" class="btn btn-danger">
                                    <span class="glyphicon glyphicon-trash" aria-hidden="true"> Excluir</span>
                                </button>   
                            </div>
                        </div>
                        <div>
                            <a asp-controller="Curso"  asp-action="Index">Voltar à lista</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>

