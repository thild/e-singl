@model Singl.Models.Departamento

@{
    ViewBag.Title = $"Detalhes do departamento \"{@Model.Nome}\"";
    Layout = "/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<section>
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h2 class="section-heading">@ViewBag.Title</h2>
                <hr class="hard"/>
                <div>
                    <dl class="dl-horizontal">
                        <dt>
                            @Html.DisplayNameFor(model => model.Sigla)
                        </dt>
                        <dd>
                            @Html.DisplayFor(model => model.Sigla)
                        </dd>
                        <dt>
                            @Html.DisplayNameFor(model => model.Nome)
                        </dt>
                        <dd>
                            @Html.DisplayFor(model => model.Nome)
                        </dd>
                    </dl>
                </div>
                <hr class="hard"/>
                <div class="pull-right">
                    <a asp-controller="Departamento" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning"><span class="glyphicon glyphicon-edit" aria-hidden="true"></span> Editar</a>
                    <a asp-controller="Curso" asp-action="Index" asp-route-departamentoId="@Model.Id" class="btn btn-primary">
                        <span class="glyphicon glyphicon-plus-sign" aria-hidden="true"></span> Listar cursos</a> 
                </div>
                <div>
                    <a asp-controller="Departamento" asp-action="Index">Voltar à lista</a>
                </div>
            </div>
        </div>
    </div>
</section>

