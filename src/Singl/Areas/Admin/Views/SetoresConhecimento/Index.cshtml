@model IEnumerable<Singl.Models.SetorConhecimento>

@{
    ViewBag.Title = "Lista de setores de conhecimento";
    Layout = "/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<section>
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h2 class="section-heading">@ViewBag.Title</h2>
                <hr class="hard">
                <table class="table">
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Sigla)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Nome)
                        </th>
                        <th></th>
                    </tr>
                @foreach (var item in Model) {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Sigla)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Nome)
                        </td>
                        <td>
                            <div class="pull-right">
                                <!--<a asp-controller="Departamentos" asp-action="Details" asp-route-sigla="@item.Sigla" class="btn btn-default"><span class="glyphicon glyphicon-search" aria-hidden="true"></span> Detalhes</a>
                                <a asp-controller="Departamentos" asp-action="Edit" asp-route-sigla="@item.Sigla" class="btn btn-warning"><span class="glyphicon glyphicon-edit" aria-hidden="true"></span> Editar</a>
                                <a asp-controller="Departamentos" asp-action="Delete" asp-route-sigla="@item.Sigla" class="btn btn-danger">
                                    <span class="glyphicon glyphicon-trash" aria-hidden="true"></span> Excluir</a>-->
                                <a href="/admin/setoresconhecimento/@item.Sigla/@item.Cidade.Sigla" class="btn btn-default">
                                    <span class="glyphicon glyphicon-search" aria-hidden="true"></span> Detalhes</a>
                                <a href="/admin/setoresconhecimento/edit/@item.Sigla/@item.Cidade.Sigla"  class="btn btn-warning">
                                    <span class="glyphicon glyphicon-edit" aria-hidden="true"></span> Editar</a>
                                <a href="/admin/setoresconhecimento/delete/@item.Sigla/@item.Cidade.Sigla" class="btn btn-danger">
                                    <span class="glyphicon glyphicon-trash" aria-hidden="true"></span> Excluir</a>
                            </div>
                        </td>
                    </tr>
                }
                </table>
                <hr class="hard">
                <div class="pull-right">
                    <a asp-controller="SetoresConhecimento" asp-action="Create" class="btn btn-primary">
                        <span class="glyphicon glyphicon-plus-sign" aria-hidden="true"></span> Criar setor de conhecimento</a>
                </div>
            </div>
        </div>
    </div>
</section>

