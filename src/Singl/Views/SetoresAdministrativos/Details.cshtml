@model Singl.Models.SetorAdministrativo
@{ Singl.Models.SetorAdministrativo setorAdministrativo = null; }

@{
    ViewBag.Title = $"Detalhes do setor \"{@Model.Nome}\"";
    Layout = "/Views/Shared/_InfoLayout.cshtml";
}

<section>
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h2 class="section-heading">@ViewBag.Title</h2>
                <hr class="hard"/>
                <div>
                    <dl class="dl-horizontal">
                        <dt>
                            @Html.DisplayNameFor(model => model.Sigla)
                        </dt>
                        <dd>
                            @Html.DisplayFor(model => model.Sigla)
                        </dd>
                        <dt>
                            @Html.DisplayNameFor(model => model.Nome)
                        </dt>
                        <dd>
                            @Html.DisplayFor(model => model.Nome)
                        </dd>
                    @if(Model.SubSetores.Count != 0) {
                        <dt>Subsetores</dt>
                        <dd>
                            <table class="table">
                                <tr>
                                    <th>
                                        @Html.DisplayNameFor(model => setorAdministrativo.Sigla)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => setorAdministrativo.Nome)
                                    </th>
                                    <th></th>
                                </tr>
                            @foreach (var item in Model.SubSetores) {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Sigla)
                                    </td>
                                    <td>
                                        <a href="/setoresadministrativos/@item.Sigla">@item.Nome</a>
                                    </td>
                                    <td>
                                        <div class="pull-right">
                                            <a href="/setoresadministrativos/@item.Sigla" class="btn btn-default">
                                                <span class="glyphicon glyphicon-search" aria-hidden="true"></span> Detalhes</a>
                                        </div>
                                    </td>
                                </tr>
                            }
                            </table>
                        </dd>
                    }
                    </dl>
                </div>
                <hr class="hard"/>
                <div>
                    <a href="javascript:history.go(-1)">Voltar</a>
                </div>      
            </div>
        </div>
    </div>
</section>

